plugins {
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.0.8'
}

dependencies {
    implementation project(':server')
    compile project(':network')
}

mainClassName = 'com.chatapp.client.ClientWindow'

javafx {
    version = "11.0.2"
    modules = [ 'javafx.controls' ]
}